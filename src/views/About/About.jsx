import React from 'react'

import pageWithLayout from '../../containers/page'

const About = props => (
    <div className='home'>
        This APP using The Movie Database (TMDd) API
    </div>
)

export default pageWithLayout(About)